# Migration `20201025172943-twitter-api`

This migration has been generated by Daniel Büchele at 10/25/2020, 5:29:43 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "public"."User.twitterId_unique"

ALTER TABLE "public"."User" DROP COLUMN "twitterId",
ADD COLUMN "createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
ADD COLUMN "twitterAccountId" text   ,
ALTER COLUMN "displayName" DROP NOT NULL

CREATE TABLE "public"."TwitterAccount" (
"id" text   NOT NULL ,
"token" text   ,
"secret" text   ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "TwitterAccount.id_unique" ON "public"."TwitterAccount"("id")

CREATE UNIQUE INDEX "User.twitterAccountId_unique" ON "public"."User"("twitterAccountId")

ALTER TABLE "public"."User" ADD FOREIGN KEY ("twitterAccountId")REFERENCES "public"."TwitterAccount"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201025155506-twitter-unique..20201025172943-twitter-api
--- datamodel.dml
+++ datamodel.dml
@@ -4,20 +4,29 @@
 }
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 model User {
-  id             String  @default(uuid()) @id
-  handle         String
-  displayName    String
-  twitterId      Int?    @unique
-  profilePicture String?
-  Share          Share[]
+  id               String          @default(uuid()) @id
+  handle           String
+  displayName      String?
+  profilePicture   String?
+  createdAt        DateTime        @default(now())
+  Share            Share[]
+  twitterAccountId String?         @unique
+  TwitterAccount   TwitterAccount? @relation(fields: [twitterAccountId], references: [id])
 }
+model TwitterAccount {
+  id     String  @unique @id
+  token  String?
+  secret String?
+  User   User
+}
+
 model Share {
   id        String   @default(uuid()) @id
   authorId  String
   author    User     @relation(fields: [authorId], references: [id])
```


