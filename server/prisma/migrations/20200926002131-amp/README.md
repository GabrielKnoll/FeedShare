# Migration `20200926002131-amp`

This migration has been generated by Daniel Büchele at 9/26/2020, 1:21:31 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Episode" ADD COLUMN "description" text   ,
ADD COLUMN "ampId" integer   ,
ADD COLUMN "durationSeconds" integer   ,
ADD COLUMN "url" text   ,
ADD COLUMN "ampApiResponse" jsonb   

ALTER TABLE "public"."Podcast" ADD COLUMN "ampId" integer   ,
ADD COLUMN "publisher" text   NOT NULL ,
ADD COLUMN "description" text   ,
ADD COLUMN "feed" text   NOT NULL ,
ADD COLUMN "url" text   ,
ADD COLUMN "ampApiResponse" jsonb   
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200924001419-share..20200926002131-amp
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 }
 datasource db {
   provider = "postgres"
-  url = "***"
+  url = "***"
 }
 model User {
   id     String  @default(uuid()) @id
@@ -26,17 +26,28 @@
   createdAt DateTime @default(now())
 }
 model Episode {
-  id        String  @default(uuid()) @id
-  title     String
-  share     Share[]
-  podcast   Podcast @relation(fields: [podcastId], references: [id])
-  podcastId String
+  id              String  @default(uuid()) @id
+  title           String
+  share           Share[]
+  podcast         Podcast @relation(fields: [podcastId], references: [id])
+  podcastId       String
+  description     String?
+  ampId           Int?
+  durationSeconds Int?
+  url             String?
+  ampApiResponse  Json?
 }
 model Podcast {
-  id      String    @default(uuid()) @id
-  title   String
-  Episode Episode[]
-  Share   Share[]
+  id             String    @default(uuid()) @id
+  title          String
+  Episode        Episode[]
+  Share          Share[]
+  ampId          Int?
+  publisher      String
+  description    String?
+  feed           String
+  url            String?
+  ampApiResponse Json?
 }
```


