query Profile($id: ID!) {
  node(id: $id) {
    ... on User {
      profilePicture(size: 80, scale: 2)
      displayName
      followers {
        totalCount
      }
      following {
        totalCount
      }
    }
  }
}
