query Feed($after: String, $feedType: FeedType) {
  shares(last: 20, after: $after, feedType: $feedType){
    edges {
      cursor
      node {
        id
        ...ShareFragment
      }
    }
  }
}
