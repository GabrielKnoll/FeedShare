query EpisodeOverlay($id: ID!) {
  node(id: $id) {
    ...on Episode {
      description
      artwork(size: 100, scale: 2)
      podcast {
        feed
        artwork(size: 100, scale: 2)
      }
    }
  }
}
