query FeedStream($after: String) {
  shares(last: 20, after: $after){
    pageInfo {
      endCursor
    }
    edges {
      cursor
      node {
        id
        ...ShareFragment
      }
    }
  }
}
